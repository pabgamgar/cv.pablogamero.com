---
import type { ProjectEntry } from "../data/cv";

interface Props {
    heading: string;
    entries: ProjectEntry[];
}
const { heading, entries } = Astro.props;
---

<div id="projects" class="flex flex-col gap-3">
    <h2>{heading}</h2>
    {entries.map((entry) => (
        <div class="flex flex-col gap-0.5">
            <h3>
                <span class="bold">{entry.name}</span> · {entry.tagline}
                <span class="date">{entry.date}</span>
            </h3>
            <ul>
                {entry.bullets.map((bullet) => (
                    <li set:html={bullet} />
                ))}
            </ul>
        </div>
    ))}
</div>
