---
interface Props {
    heading: string;
    text: string;
}
const { heading, text } = Astro.props;
---

<div id="professional-exp" class="flex flex-col gap-3">
    <h2>{heading}</h2>
    <p set:html={text} />
</div>
